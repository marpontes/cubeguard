cubeguard.roleMapperClass=com.oncase.olap.mapper.DynamicUserRoleMapper
cubeguard.usernameVar=${NOME_USUARIO}
cubeguard.dspClass=com.oncase.olap.security.DynamicMappedRolesSchemaProcessor
cubeguard.replaceString=true
cubeguard.stringToBeReplaced=${USER_NAME}
cubeguard.rulesEndpointPath=/getcustomaccessrulessteelwheels
cubeguard.defaultEndpointName=getcustomaccessrules
cubeguard.defaultEndpointType=resultset
cubeguard.debug=false

# Here you can list as many session variables as you want, separated by ,
# Each session var must be written with the notation - ${VARNAME}
# example: ${PERMISSIONS_INJECTOR},${MAIN_ROLE}
cubeguard.sessionVarsToReplace=
